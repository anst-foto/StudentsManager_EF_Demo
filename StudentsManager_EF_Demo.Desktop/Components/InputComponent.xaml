<UserControl x:Class="StudentsManager_EF_Demo.Desktop.Components.InputComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Component">
    <Grid DataContext="{Binding ElementName=Component}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <Label Content="{Binding Path=Label, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
               Target="{Binding ElementName=Input}"
               Grid.Column="0"/>
        <TextBox x:Name="Input"
                 Text="{Binding Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 IsReadOnly="{Binding Path=IsReadOnly, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                 Grid.Column="1"/>
    </Grid>
</UserControl>
